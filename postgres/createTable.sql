CREATE TABLE computers (
    device_type         TEXT,
    brand               TEXT,
    model               TEXT,
    release_year        INT,
    os                  TEXT,
    form_factor         TEXT,
    cpu_brand           TEXT,
    cpu_model           TEXT,
    cpu_tier            INT,
    cpu_cores           INT,
    cpu_threads         INT,
    cpu_base_ghz        NUMERIC(4,2),
    cpu_boost_ghz       NUMERIC(4,2),
    gpu_brand           TEXT,
    gpu_model           TEXT,
    gpu_tier            INT,
    vram_gb             INT,
    ram_gb              INT,
    storage_type        TEXT,
    storage_gb          INT,
    storage_drive_count INT,
    display_type        TEXT,
    display_size_in     NUMERIC(5,2),
    resolution          TEXT,
    refresh_hz          INT,
    battery_wh          NUMERIC(6,2),
    charger_watts       INT,
    psu_watts           INT,
    wifi                TEXT,
    bluetooth           TEXT,
    weight_kg           NUMERIC(5,2),
    warranty_months     INT,
    price               NUMERIC(10,2)
);
